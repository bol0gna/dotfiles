source ~/.util/rcload.sh

rcload ~/.util/env_xdg.sh

SHELL_SESSIONS_DISABLE=1
ZDOTDIR="$XDG_CONFIG_HOME/zsh"
# [ -d "$XDG_CACHE_HOME/zsh" ] || mkdir -p "$XDG_CACHE_HOME/zsh"

export FPATH="$ZDOTDIR/plugins:$FPATH"

